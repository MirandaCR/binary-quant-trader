# binary-quant-trader

**Hybrid AI trading system for IQ Option binary options** — combines **traditional machine learning**
with **generative AI (LLM)**, a live risk manager, and a real-time Next.js dashboard.

📄 *Spanish version: [README.es.md](README.es.md)* · 📚 *Full docs in [`docs/`](docs/)*

> ### ⚠️ Read this first — honest disclaimer
>
> This is a **research and educational project**, not a money-making machine, and it is not
> financial advice.
>
> - **Binary options are a negative-expectancy instrument.** With an ~80% payout you need to win
>   **more than ~55.6%** of trades just to break even. The broker is designed to have the edge.
> - **OTC assets are synthetic.** On weekends/OTC, the price feed is generated by the broker
>   itself — you are betting against the party that creates the numbers and can see your position.
> - **Automated trading violates most brokers' Terms of Service** (IQ Option included) and can get
>   your account frozen. The anti-fingerprinting measures here **reduce** obvious tells; they do
>   **not** remove that risk.
> - **Backtest results are idealized** (no spread/slippage, constant payout, small samples prone to
>   overfitting). A high backtest win rate does **not** predict live profit.
>
> Use it on a **PRACTICE (demo) account** to learn about trading-system architecture, LLM agents,
> and ML meta-labeling. Trading real money is at your own risk. See [Honest Limitations](#honest-limitations).

---

## 📚 Documentation

| Doc | What's inside |
|-----|---------------|
| 🚀 **[Getting Started (for dummies)](docs/GETTING_STARTED.md)** | Zero-assumptions, step-by-step setup: what to install, how to get API keys, how to run and read the dashboard, troubleshooting. |
| 🏗️ **[Architecture](docs/ARCHITECTURE.md)** | System & engine diagrams (Mermaid), component responsibilities, one-trade sequence, directory map. |
| 🤖 **[AI Workflow (end to end)](docs/AI_WORKFLOW.md)** | How the two AIs work together — LLM agents that *write* strategies + ML that *judges* them — with full diagrams. |

---

## What it actually is

A well-structured example of how to combine several ideas into one live trading loop:

- A **strategy engine** that backtests 21 built-in technical strategies across many assets and ranks them.
- A **generative-AI layer** (LLM agents) that *writes and improves* new strategy code from market news + live performance.
- A **traditional ML layer** (logistic-regression meta-labeling) that learns from your *real* trade
  history which strategy/asset/time/confidence combos actually win, and adjusts each signal's confidence.
- A **dynamic portfolio** that trades several assets concurrently, splitting capital by score.
- A **risk manager** with soft warnings, a hard stop, and human-like trade jitter.
- A **real-time dashboard** (Next.js) showing trades, equity, strategies, agents, news, and the live console.

Good software engineering and a profitable edge are **different things** — this project delivers the
former honestly and makes no promises about the latter.

## The two AIs (and why there are two)

The heart of this project is that it uses **two completely different kinds of AI that do opposite jobs**.
Most "AI trading bots" use one model and hope. This one uses two, and makes them check each other.

| | 🧠 Generative AI (LLM) | 📊 Traditional ML |
|--|------------------------|-------------------|
| **Its job** | **CREATES** the strategies | **JUDGES** the signals |
| **What it does** | Reads market news and *writes brand-new Python strategy code from scratch*, backtests it, keeps the winners, and rewrites the losers. | Learns from your *real* closed trades which strategy/asset/time combos actually win, and turns each signal's confidence up or down accordingly. |
| **Tech** | DeepSeek / OpenAI / Gemini / Claude (swappable) | Logistic Regression or XGBoost (selectable) |
| **Where you see it** | The **AI Agents** tab — six agents working live | The **"How good is the AI, really?"** panel on the Overview tab |
| **Personality** | The confident creative who invents ideas | The skeptic who's seen every idea fail before |

**In one sentence:** the generative AI is the *engine that invents and evolves* the strategies you trade;
the traditional ML is the *filter that decides how much to trust each one*. Remove the generative AI and
the bot is stuck with its 21 built-in strategies forever, never adapting. Remove the ML and every signal
is taken at face value, even the ones history says are lying. They need each other.

> This is the classic quant technique called **meta-labeling**: never let one model both generate *and*
> validate its own ideas — bring in a second, skeptical model to grade the first one's homework.

## Features

| Area | What it does |
|------|--------------|
| **Multiple LLM providers** | DeepSeek (default), OpenAI/ChatGPT, Google Gemini, Anthropic Claude — switchable in the UI. |
| **Multi-agent system** | News → Research → Backtest → Trade Analysis → Parameter Optimizer, generating and pruning strategies live. |
| **ML signal scorer** | Meta-labeling trained on your closed trades (activates after 30+). Model is selectable: `logistic`, `xgboost`, or `auto` (upgrades logistic→XGBoost as data grows). |
| **Plain-English model report card** | The dashboard translates model performance into "the AI guessed 62% vs 54% for a coin flip" — and honestly says so when it's *not* beating the coin. |
| **Dynamic portfolio** | Trades up to N distinct assets per candle; capital split by backtest score (not multiplied). |
| **Risk management** | Daily-loss / consecutive-loss soft limits, compound interest option, hard stop kill-switch. |
| **Anti-fingerprinting** | Randomized entry timing and ±3% position-size jitter to avoid perfectly-periodic bot patterns. |
| **Live asset discovery** | Probes the broker for which assets are actually open right now. |
| **Dashboard** | Trades, equity curve, P&L calendar, strategy leaderboard, agent flow, news, console. |

## Architecture

```
┌──────────────────────────┐        WebSocket + REST        ┌──────────────────────────┐
│   Next.js dashboard        │  ◄──────────────────────────►  │   FastAPI backend          │
│   (port 3010)              │                                │   (port 8100)              │
└──────────────────────────┘                                └────────────┬─────────────┘
                                                                          │
                        ┌─────────────────────────────────────────────────┼─────────────────────┐
                        │                        Trading Engine            │                     │
                        │  ┌───────────┐  ┌────────────┐  ┌─────────────┐ │ ┌─────────────────┐ │
                        │  │ Strategy  │  │  ML signal  │  │  Portfolio  │ │ │  Risk manager   │ │
                        │  │ backtester│  │  scorer     │  │  selector   │ │ │  + kill switch  │ │
                        │  └───────────┘  └────────────┘  └─────────────┘ │ └─────────────────┘ │
                        └──────────────┬──────────────────────────────────┴─────────────────────┘
                                       │
              ┌────────────────────────┼──────────────────────────┐
              │                        │                          │
     ┌────────▼────────┐     ┌─────────▼─────────┐      ┌──────────▼──────────┐
     │  LLM providers  │     │   IQ Option API   │      │      NewsAPI        │
     │ deepseek/openai │     │  (candles, buy,   │      │  (market headlines) │
     │ gemini/anthropic│     │   check_win)      │      │                     │
     └─────────────────┘     └───────────────────┘      └─────────────────────┘
```

## Tech stack

- **Backend:** Python 3.11, FastAPI, SQLAlchemy (SQLite), scikit-learn, pandas/numpy, httpx.
- **Frontend:** Next.js 14, React, TypeScript, Tailwind CSS, Recharts.
- **Broker API:** [`iqoptionapi`](https://github.com/iqoptionapi/iqoptionapi).

## Prerequisites

- **Python 3.11+**
- **Node.js 18+**
- **Git** (the IQ Option API is installed from GitHub)
- An **IQ Option account** (use a PRACTICE/demo account)
- Optional: an **LLM API key** (DeepSeek by default) and a **NewsAPI** key

> **Note:** Do not run installs from a cloud-synced folder (Google Drive / OneDrive virtual drives).
> The heavy file churn of `npm install` / `pip install` corrupts `node_modules` and `venv` on those
> virtual filesystems. Keep the project on a **local disk**.

## Setup & run

### Quick start (both servers)

```bash
python run.py
```

This creates the Python venv, installs dependencies, runs `npm install`, and starts:

- Backend → http://localhost:8100
- Frontend → http://localhost:3010

### Manual (two terminals)

```bash
# Terminal 1 — backend
cd backend
python -m venv venv
venv/Scripts/python -m pip install -r requirements.txt   # Windows
python -m uvicorn main:app --port 8100

# Terminal 2 — frontend
cd frontend
npm install
npm run dev        # http://localhost:3000
```

## Configuration

Credentials can be entered directly in the dashboard's config panel, or via environment variables.
Copy `backend/.env.example` to `backend/.env`:

```env
IQ_EMAIL=your_email@example.com
IQ_PASSWORD=your_password
ACCOUNT_TYPE=PRACTICE            # PRACTICE or REAL

# AI provider: deepseek (default) | openai | gemini | anthropic
AI_PROVIDER=deepseek
AI_API_KEY=your_provider_key
# Optional overrides — leave empty to use the provider's defaults
AI_BASE_URL=
AI_MODEL=

NEWS_API_KEY=your_newsapi_key    # optional
```

> **Security:** never commit real credentials. Keep `.env` out of version control, and rotate any key
> that was ever committed or synced to the cloud.

## How it works (one trading cycle)

1. **Connect** to IQ Option and probe which configured assets are actually open.
2. **Backtest** all strategies × assets on recent candles; rank by a composite score
   (win rate, profit factor, drawdown, activity).
3. **Select a portfolio** of the top distinct-asset combos (default 3) and split capital by score.
4. Just before each candle close, **generate signals**; the **ML scorer** blends each strategy's
   confidence with its historical win-probability.
5. **Enter trades** with randomized sub-second timing and jittered size, sized by the risk manager.
6. **Record results**, retrain the ML scorer, and rotate out combos that lose live.
7. In parallel, **LLM agents** research news, write new strategy code, backtest it, and inject the
   winners — pruning the losers.

## Honest limitations

This section exists on purpose. If you share or fork this, keep it.

- **The math is against you.** Sustained profit on binary options — especially OTC — is extremely
  difficult by design. No technical strategy has a guaranteed edge over a broker-controlled feed.
- **Overfitting risk.** Strategies are selected as "best of many" on small candle samples, with
  progressively looser thresholds. That can promote noise that merely *looks* like signal.
- **Backtest ≠ live.** The backtester has **no look-ahead bias** (verified — strategies never see the
  outcome candle). Exact ties are settled as refunds (not losses), and an optional `slippage` band
  models entry latency/spread. It's still idealized, though: constant 0.80 payout by default and a
  one-candle backtest/live alignment gap remain.
- **Account/TOS risk.** Automated trading breaks broker terms. Anti-fingerprinting reduces obvious
  tells but does not guarantee you won't be detected or restricted.
- **ML on small data.** With only dozens–hundreds of trades, the meta-labeling model is a gentle
  nudge, not an oracle.

**Bottom line:** treat this as a learning platform for AI + trading-system engineering. Run it on a
demo account. Do not risk money you can't afford to lose.

## Project structure

```
backend/
  main.py                 FastAPI app (REST + WebSocket)
  config/                 settings + asset catalog
  connection/             IQ Option client wrapper
  engine/                 trading engine (portfolio, live loop, rotation)
  strategies/             21 built-in technical strategies
  backtesting/            walk-forward backtester + ranking
  ml/                     traditional ML signal scorer (meta-labeling)
  agents/                 LLM providers + multi-agent orchestrator
  risk/                   risk manager + kill switch
  news/                   NewsAPI fetcher
  database/               SQLAlchemy models + SQLite access
frontend/
  app/                    Next.js pages
  components/             dashboard UI (portfolio, trades, agents, strategies…)
  hooks/                  bot state + WebSocket hooks
run.py                    one-command launcher for both servers
```

## Contributing

Issues and PRs welcome — especially around backtest realism (spread/slippage modeling, tie handling),
strategy quality, and honest reporting. Please keep the disclaimers intact.

## License

Licensed under the **GNU Affero General Public License v3.0 (AGPL-3.0)** — see [LICENSE](LICENSE).

This is a strong copyleft license chosen on purpose: anyone who uses, modifies, or **runs a modified
version as a network service** must release their source code under the same terms and keep the honest
disclaimers intact. It exists to prevent this project from being repackaged into a closed-source
"guaranteed profit" scam.

---

*This project is for educational purposes. It is not financial advice. Trading binary options carries
a high risk of losing your capital. The authors accept no responsibility for financial losses or
account actions taken by any broker.*
